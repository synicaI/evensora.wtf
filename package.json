{
  "name": "evensora-lol-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page for evensora.lol",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
